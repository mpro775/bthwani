"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = __importDefault(require("express"));
const controller = __importStar(require("../../controllers/delivry/DeliveryOrderController"));
const verifyAdmin_1 = require("../../middleware/verifyAdmin");
const verifyAdminOrDriver_1 = require("../../middleware/verifyAdminOrDriver");
const verifyFirebase_1 = require("../../middleware/verifyFirebase");
const express_validator_1 = require("express-validator");
const createOrderValidators = [
    (0, express_validator_1.body)('addressId').isMongoId(),
    (0, express_validator_1.body)('city').optional(), // للتوافق العكسي إن أردت
    (0, express_validator_1.body)('latitude').optional(), // لن تحتاجها بعد
    (0, express_validator_1.body)('longitude').optional(), // لن تحتاجها بعد
];
const router = express_1.default.Router();
router.use(verifyFirebase_1.verifyFirebase);
router.post('/', createOrderValidators, (req, res, next) => __awaiter(void 0, void 0, void 0, function* () {
    const errors = (0, express_validator_1.validationResult)(req);
    if (!errors.isEmpty()) {
        res.status(400).json({ errors: errors.array() });
        return;
    }
    next();
}), controller.createOrder);
router.get('/user/:userId', controller.getUserOrders);
router.get('/:id', controller.getOrderById);
router.get('/', verifyAdmin_1.verifyAdmin, controller.getAllOrders); // للأدمن مع فلترة
router.put('/:id', verifyAdminOrDriver_1.verifyAdminOrDriver, controller.updateOrderStatus);
exports.default = router;
